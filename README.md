Setup

 ~/.zshrc 
```
. "$HOME/.local/bin/env"
. "[path to lib]/dot-files/.zshrc"
```

~/.tmux.conf
```
source-file [path to lib]/dot-files/.tmux.conf
```

## neovim

link ~/.config/nvim to [path to lib]/dot-files/nvim
